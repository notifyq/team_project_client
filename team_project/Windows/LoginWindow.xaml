<Window x:Class="team_project.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:team_project.Windows"
        mc:Ignorable="d"
        Title="Авторизация" MinHeight="450" MinWidth="800" Loaded="Window_Loaded">
    <Grid Background="{StaticResource WindowBackground}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="9*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Grid Background="Black" Grid.ColumnSpan="3">
            
        </Grid>
        <Grid Background="Black" Grid.ColumnSpan="3" Grid.Row="2">

        </Grid>

        <Frame Name="GuestFrame" NavigationUIVisibility="Hidden" Background="{StaticResource LoginBackground}" Grid.RowSpan="1" Grid.Column="1" Grid.Row="1"></Frame>
    </Grid>
</Window>
